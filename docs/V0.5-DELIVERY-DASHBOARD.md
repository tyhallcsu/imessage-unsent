# v0.5 Delivery Dashboard

Living tracker for the v0.5.0 release cycle (branding integrity + GUI design pass +
fresh engineering/UX audit). Updated after every milestone. Started 2026-07-20.

## Baseline truth (verified 2026-07-20, read-only reconstruction)

| Fact | Verified value |
| --- | --- |
| Default branch | `main` @ `7015b51` (local == `origin/main`) |
| Latest stable release | **v0.4.0**, published 2026-07-20T21:20:11Z, marked Latest, not draft |
| v0.4.0 tag commit | `d1370ba` (matches handoff claim) |
| v0.4.0 assets | source tar.gz, GUI zip, daemon arm64 tar.gz — each with `.sha256` |
| Open PRs | none |
| Open issues | #127 (actionlint), #96 (audit tracker), #88/#26/#25/#22/#16/#15 (roadmap + ethics-gated) |
| Post-release commits on main | #133 (handoff doc), #134 (icon source exports), #136 (README banner+icon) |
| CI | green on main and all recent PRs; 7 parallel jobs |
| Installed GUI | `/Applications/iMessage Unsent.app` = 0.4.0 (CFBundleShortVersionString + CFBundleVersion) |
| Installed daemon | `~/Library/Application Support/imessage-unsent/bin/imu-watcher` reports version=0.4.0; LaunchAgent running (last exit 0, no crash loop) |
| Git identity in repo | `tyhallcsu <tyhallcsu@users.noreply.github.com>` (correct) |
| gh account at session start | a non-repo default account was active; every GitHub write bundles an explicit switch to `tyhallcsu` |
| Stranded branch check | local `feat/gui-about-window` is stale (its PRs #63/#84 were CLOSED; About window landed on main separately). No work to salvage. |
| Creator attribution | **verified**: LICENSE = "Copyright (c) 2026 sharmanhall"; maintainer handle `sharmanhall` per AGENTS.md. "Created by sharmanhall" stays. |

## Required branding fixes

| Item | Status | Evidence |
| --- | --- | --- |
| README displays `assets/MacOS_AppIcon_iMessage_Unsent.png` instead of `icon.svg` | **already done on main** | `README.md:3` (merged PR #136); repo-wide grep shows no `icon.svg` reference in README/CLAUDE/AGENTS/docs-architecture; remaining `icon.svg` mentions are dated historical docs (project-review-2026-05-04, handoffs) |
| Source icon asset validity | verified | 1254×1254 PNG, RGBA, hasAlpha=yes |
| Finder/Dock app icon | already wired | `scripts/build-app-icon.sh` → `AppIcon.icns` → staged by both `scripts/build-release.sh:116-118` and `script/build_and_run.sh`; `CFBundleIconFile=AppIcon` in `gui/Info.plist` |
| **About window icon** | **TO DO** | `gui/Sources/IMUMenuBar/Views/AboutWindow.swift:9-14` renders `Image(systemName: "message.fill")` placeholder |
| Packaging gate for icon resource | TO DO | `scripts/rc_smoke.sh` `GUI_REQUIRED_ENTRIES` lacks `Contents/Resources/AppIcon.icns`; no bats coverage of icon generation |
| icon.svg deletion | **do not delete** | still referenced by historical docs; harmless; out of scope |

## GUI screens audited (Phase 3)

| Screen | Read | Findings recorded | Fixed |
| --- | --- | --- | --- |
| About window | yes | placeholder icon, dead lower space, weak hierarchy | **#138 merged** |
| Menu bar label (StatusIconView) | yes | status by color-dot only; no attention shape | #139 PR (attention glyph + a11y summary) |
| Menu bar menu (MenuBarContentView) | yes | FDA-lost/daemon-down invisible on primary surface; no shortcuts | #139 PR (warning rows, ⌘, ⌘Q) |
| History window | yes | rows not keyboard/VoiceOver activatable; empty state not actionable | #139 PR (Button rows, Health-Check CTA) |
| Recovery detail sheet | yes | failed delete silent; unlabeled close; gray-opacity fills | #139 PR (lastActionError chip, a11y label, .quaternary) |
| Settings window | yes | http:// webhook accepted silently | #139 PR (insecure-webhook warning) |
| Doctor window | yes | solid: multi-channel severity, remediation links | no change needed |

Visual verification: 16 offscreen renders (dark+light) of menu (healthy/FDA-denied/down),
History (populated/down-empty), detail (recovered/not-recoverable/delete-failed) — all
synthetic data; List rendering proven via offscreen NSWindow hosting. About before/after
renders captured for #138. Live-window screenshot capture blocked by missing Screen
Recording TCC for the terminal (environmental; not altered).

## Audit findings (fresh v0.5 pass)

Prior review (docs/FABLE5-ULTRACODE-REVIEW.md) status inherited: F-H1..H5 fixed;
F-M1/M3/M6/M7/M8/M9/M11 fixed; **F-M2 doc-reconciled**; **still open: F-M4, F-M5,
F-M10, F-L1..F-L13**. The GUI/UX/accessibility dimension was never audited in that
pass (review agent terminated) — this cycle covers it.

New findings this cycle: recorded in issues as they are verified (see below).
- Candidate (to verify): `imu-watcher --version` does not print-and-exit; any argv
  starts the watcher (observed live during baseline verification).
- Doc drift (verified): `docs/release-process.md:104` claims the release workflow
  re-runs Swift tests; `release.yml` deliberately does not (CI-on-main + ancestry
  guard is the gate per release.yml:107-109).

## Issues / branches / PRs this cycle

| # | Title | Branch | PR | CI | Merged |
| --- | --- | --- | --- | --- | --- |
| – | (to be filed) About-window branding integrity | feat/N-about-branding | – | – | – |

## Release readiness (v0.5.0)

- [ ] Branding PR merged
- [ ] GUI design-pass PR merged
- [ ] Audit High/Medium fixes merged (with regression tests)
- [ ] Docs reconciliation merged
- [ ] `imuDaemonVersion` bumped to 0.5.0 (sole hand-maintained version source; workflow drift-guards it)
- [ ] `make rc-smoke` green locally
- [ ] Tag v0.5.0 on merged main commit; Release workflow green
- [ ] Draft release verified (assets, checksums, icns inside zip) then published as Latest
- [ ] Local install of published artifacts; both components report 0.5.0
- [ ] Rollback path recorded (v0.4.0 assets remain downloadable; installed-app backup kept)

## Blockers / risks

- Apple signing secrets absent (0/6) → artifacts ad-hoc signed/unsigned, Gatekeeper
  warns on first launch. Documented, not a blocker; never claim notarization.
- FDA for a rebuilt daemon binary requires a manual toggle in System Settings
  (TCC is per code identity). Will report exact step post-install; will NOT touch TCC.

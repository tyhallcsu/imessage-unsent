# v0.5 cycle handoff — living source of truth

Keep this document current after **every** milestone (PR opened, PR merged,
issue filed, validation run). Any AI/session resuming the v0.5 cycle starts
here, verifies against git/GitHub, and continues the queue below.
Last updated: 2026-07-20 (PRs #145+#146 merged; #143 fix implemented, PR opening).

## Ground rules (non-negotiable, see AGENTS.md/CLAUDE.md)

- Never push to `main`; one issue + PR per milestone; squash-merge only.
- Commit identity `tyhallcsu <tyhallcsu@users.noreply.github.com>` (repo-local).
- GitHub writes require the `tyhallcsu` gh account; the active account on this
  machine reverts to a non-repo default between shells — bundle
  `gh auth switch --hostname github.com --user tyhallcsu` into the same shell
  command as every push/merge/issue/release write.
- No PII: handles `sharmanhall`/`tyhallcsu` only; tests use `/Users/example`;
  scan every diff before committing.
- Swift tests need full Xcode: prefix commands with
  `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`.
- Daemon concurrency changes ⇒ run the suite 3× + once under
  `swift test --sanitize=thread`.
- Read-only against `~/Library/Messages`; deterministic fixtures only; never
  touch TCC/FDA/Gatekeeper; Apple signing secrets are ABSENT (0/6) — artifacts
  are ad-hoc/unsigned, never claim notarization.

## Mission

Ship v0.5.0: real About icon (done), GUI design+accessibility pass (done),
fresh three-domain audit (done — findings below), fix all verified High/Medium
findings + cheap high-value Lows, docs reconciliation, then release + local
install. Full mission text lives in the session brief; the queue below is the
actionable remainder.

## State (verify with `gh pr list` / `git log origin/main` before relying)

- v0.4.0 = latest published release, tag `d1370ba`. Installed GUI + daemon 0.4.0.
- main @ `33a7fd4` (after PR #145).
- **Merged this cycle:** #138 (About icon + packaging gates, closes #137),
  #140 (GUI design/a11y pass, closes #139), #145 (daemon socket robustness,
  closes #141 — D-1+D-2, 7 new tests, suite 104/1/0 ×3 + TSan clean).
- #146 merged (retry semantics, closes #142 — D-3/F-M4, 4 new tests).
- **In flight:** `fix/143-wal-buffer-hygiene` (D-4+D-5): archiveTo age-filters
  to maxAge; trimExpired() from the heartbeat; detect/archive moved to a
  serial pipelineQueue (snapshot-first preserved; refs captured on the
  watcher queue). 3 snapshotter tests updated/added. Suite 110/1/0 x3 + TSan
  clean; E2E proves the split still archives end-to-end.
- Issues filed: #141 (D-1+D-2), #142 (D-3), #143 (D-4+D-5), #144 (D-6/8/9/11).
- GUI suite currently 137 tests / 0 failures.

## Audit findings index

Three audit reports were produced in-session (GUI = G-*, daemon = D-*,
release/shell/docs = R-*). Verified severities; full details were recorded in
the issue bodies (#141–#144) and PR bodies (#138/#140/#145); remaining items
are summarized inline in the queue below. A consolidated
`docs/V0.5-AUDIT-REPORT.md` is owed by the docs milestone (queue step 6 below).

## Remaining queue (in order)

1. **Merge #143 PR** (in flight): CI → squash-merge → delete branch.
2. **fix/144-config-compactor** (issue #144, Lows worth fixing): ConfigStore
   parser truncates quoted values at `#` (fix quote-aware comment strip; same
   bug exists GUI-side in `ConfigFileStore.parse` — fix + test both);
   config.toml 0600 daemon-side; ArchiveCompactor partial-failure state
   ("partial", retryable — new ArchiveCompactorTests with throwing FileManager);
   webhook https-only enforcement (allow localhost http) in RecoveryNotifier +
   spy test.
3. **GUI reliability** (file new issue; Mediums/Highs from GUI audit):
   G-2 socket I/O off the main actor (2 s poll blocks UI up to ~3-4 s per tick
   when daemon wedges; `compactAll` worst case ~200 blocking round-trips) —
   async refresh + async compactAll with progress; G-3 bootstrap
   `model.start()` + RecoveryWatcher in `applicationDidFinishLaunching`
   (MenuBarExtra `.menu` content `.onAppear` may not fire until first click);
   G-4 `RecoveryWatcher` must honor the `previewChars` privacy setting
   (currently hardcodes 120-char banner bodies; 0 ⇒ no text) — closure like
   `isEnabled`, tests via injected notifier.
4. **Shell/python forensic fixes** (file new issue; from release audit):
   R-4 dedup by full text (not `text[:120]`) in `wal_extract.py` +
   `wal_merge_candidates.py` + pytest both; R-5 GUID count prints 1 when 0
   (`recover.sh:520`); R-6 newline-bearing text breaks the pretty TSV path
   (escape in `wal_extract.py` print; consume in `recover.sh`); R-11
   `make rc-smoke OUTPUT_DIR=… ` without VERSION misparses (env-only recipe);
   R-12 `script/*.sh` missing from SHELL_SOURCES (shellcheck gap); R-13 EOF-
   window silent drop warning in `wal_extract.py`.
5. **Release-eng fixes** (file new issue): R-1 shipped tarball `install.sh`
   cannot work standalone (make installer prefer sibling prebuilt
   `./imu-watcher` + `./com.imu.watcher.plist`, skip `swift build`; bats:
   extract tarball → install under fake HOME); R-2 `sign-release.sh` writes
   .p12/keychain to predictable `/tmp` when RUNNER_TEMP unset (mktemp + umask
   077); R-8 release-notes hardcoded "(UNSIGNED)" footer → signing-agnostic
   wording; scope the `sed s/VERSION/` substitution.
6. **Docs truth PR** (file new issue): R-3 release-process.md rewrite (workflow
   no longer re-runs Swift tests; secrets table MISSING `APPLE_DEVELOPER_ID_NAME`
   — a by-the-book release silently ships unsigned; document the ancestry +
   version-drift gates); R-7 daemon-notification claims (README:450,
   install-daemon.sh:46 echo, architecture.md:43/82/138/152 — GUI owns
   notifications); #113 socket "read-only" drift (README:518 + :460 table,
   architecture.md:28); R-16 staleness (CLAUDE.md "latest tag v0.3.0-rc1",
   README:529 0.2.0 example payload, README:565/569 "v0.2 ships" phrasing,
   code-signing.md:105 codesign-a-tarball example); note "uninstall leaves
   archives/state/logs" in README/SECURITY; update
   docs/V0.5-DELIVERY-DASHBOARD.md; write docs/V0.5-AUDIT-REPORT.md covering
   every G-/D-/R- finding (fixed vs documented) including documented-only Lows:
   D-7 log redaction/rotation, D-10 perms residuals, D-12 process-group kill,
   D-13 startup sweep, D-14 installer nits (D-12 priority raised: its missing
   process-group kill is also the root cause of a pre-existing ~1-in-3-under-
   load flake in BoundedProcessRunnerTests.testHangingChildTimesOutIsKilled…
   — the orphaned grandchild races the `done` marker; fixing D-12 makes that
   test deterministic), R-9 workflow interpolation residue,
   R-10 img-src link-checker gap, R-14 edit-history live-db divergence, R-15
   keychain list, R-17 sqlite-error miscategorization, G-12 status-reset races,
   G-13 stats I/O in body, G-18 searchable on macOS 13, G-20 formatting/i18n,
   G-11 remainder (identifiers/announcements), G-1 deliberate cut (lastError
   not in attention state — daemon never clears it).
7. **release/v0.5.0**: bump `daemon/Sources/IMUCore/Version.swift` → "0.5.0"
   (sole load-bearing version; the release workflow drift-gates it), release
   notes, `make rc-smoke VERSION=v0.5.0-rc? / v0.0.0-smoke` full gate, PR,
   merge, tag the merged commit, Release workflow (produces DRAFT), verify
   assets + sha256 + `AppIcon.icns` inside the zip (`unzip -Z1`), publish as
   Latest, then local install: back up `/Applications/iMessage Unsent.app`,
   install published zip, install daemon from the v0.5.0 tarball path
   documented in README, relaunch, verify both report 0.5.0, verify About
   icon, report (do not fix) FDA state. Keep rollback: v0.4.0 assets remain
   downloadable.

## Environment notes for the next session

- This machine's worktree: `.claude/worktrees/imessage-unsent-v05-gui` (branch
  work happens here; `main` is checked out in the primary clone — never `cd`
  there).
- Offscreen SwiftUI snapshot harness (for visual verification without Screen
  Recording TCC, which this terminal lacks): host the view in a real but
  never-shown borderless `NSWindow` so NSTableView-backed Lists populate;
  render via `bitmapImageRepForCachingDisplay`. Rebuild it in a scratch dir if
  needed — it is deliberately not part of the repo.
- Launch Services always routes `imu://` URLs to the `/Applications` copy when
  both it and a dev build are present; kill the installed instance first when
  URL-testing a dev build (it respawns on the next `open`).
- The menu-bar app runs at login; after any `make gui-run`, restore the user's
  installed app: `open -a "/Applications/iMessage Unsent.app"`.
